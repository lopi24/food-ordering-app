import classes from "./MenuContent.module.css";

const MenuContent = () => {
  return (
    <section className={classes["catering-content-section"]}>
      <div className={classes.header}>
        <h2>Our Menu</h2>
      </div>
    </section>
  );
};

export default MenuContent;
